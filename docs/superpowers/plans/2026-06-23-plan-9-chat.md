# Español Benidorm — План 9: Чат с наставником (Implementation Plan)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Вкладка «Чат» — живой диалог с AI-наставником: он помнит профиль ученика и историю переписки, объясняет, помогает, может дать мини-упражнение и поправить испанский. История хранится в `settings.chatHistory` (синхронизируется, без миграции БД).

**Architecture:** Поверх Планов 1–8. Новый экран через реестр. AI-ответ — `chatReply(history, profile)` поверх `callClaude`, контекст = профиль (`buildProfile`) + последние сообщения. Чистая `recentMessages` (обрезка истории + гарантия, что первый ход — пользователя) с юнит-тестом.

**Tech Stack:** HTML/CSS/JS (ES-модули, без сборки), IndexedDB, Claude API, Node 20 `node --test`.

## Global Constraints

- Интерфейс на русском; без сборки; без бэкенда; ключи не коммитить.
- Любой текст (пользователь/модель) — в DOM только через `escapeHtml`.
- Ошибки AI ловятся и показываются (как сообщение в чате); обработчики после `await` проверяют, что экран на месте (`#chat-log`).
- Двойная отправка блокируется флагом `busy`.
- Модель по умолчанию `claude-haiku-4-5`.
- Чистые функции — тесты `node --test`; экран/AI — ручной чек-лист. Существующий 41 тест остаётся зелёным.
- `js/app.js` НЕ менять.
- Порядок вкладок: Учитель(5), **Чат(6)**, Учить(10), 5 слов(15), Словарь(20) — это новые 5 главных; Аудио(30), Логопед(35), Грамматика(40), Прогресс(50), Настройки(90) уходят в «Ещё».
- Существующие интерфейсы:
  - `js/app.js`: `registerFeature(...)` (нижнее меню само берёт первые 5 по `order` как главные, остальное в «Ещё»)
  - `js/claude.js`: `callClaude(...)`; импорт из util: `import { extractJson } from './util.js';`; импорт промптов одной строкой из `./prompts.js`
  - `js/profile.js`: `buildProfile()`
  - `js/db.js`: `getSetting`, `setSetting`
  - `js/util.js`: `escapeHtml`
- Рабочая директория: `/Users/nik/Downloads/EspanolBenidorm`, ветка `plan-9-chat`.

---

## File Structure (этот план)

- `js/util.js` — добавить `recentMessages(history, max)` (чистая, **тест**)
- `js/prompts.js` — добавить `CHAT_TUTOR_SYSTEM`
- `js/claude.js` — добавить `chatReply(history, profile)` (импортировать `recentMessages`, `CHAT_TUTOR_SYSTEM`)
- `js/chat.js` — экран «Чат»
- `index.html` — подключить `chat.js`
- `sw.js` — кэш `chat.js` + версия `espanol-v10`
- `css/styles.css` — стили чата
- `test/util.test.js` — тесты `recentMessages`

---

## Task 1: Обрезка истории (`js/util.js`)

**Files:**
- Modify: `js/util.js` (добавить функцию в конец)
- Modify: `test/util.test.js` (добавить тесты)

**Interfaces:**
- Produces: `recentMessages(history, max = 20): {role, content}[]` — последние `max` сообщений, приведённые к `{role, content}`; ведущие сообщения с `role !== 'user'` отбрасываются (Anthropic API требует, чтобы первый ход был пользовательским).

- [ ] **Step 1: Добавить тесты в конец `test/util.test.js`**

```js
import { recentMessages } from '../js/util.js';

test('recentMessages: берёт последние max и только role/content', () => {
  const h = [];
  for (let i = 0; i < 25; i++) h.push({ role: i % 2 ? 'assistant' : 'user', content: 'm' + i, ts: i });
  const out = recentMessages(h, 10);
  assert.equal(out.length <= 10, true);
  assert.deepEqual(Object.keys(out[0]).sort(), ['content', 'role']);
});

test('recentMessages: первый элемент всегда user', () => {
  const h = [{ role: 'assistant', content: 'a' }, { role: 'user', content: 'b' }, { role: 'assistant', content: 'c' }];
  const out = recentMessages(h, 10);
  assert.equal(out[0].role, 'user');
  assert.equal(out[0].content, 'b');
});

test('recentMessages: пустая история → []', () => {
  assert.deepEqual(recentMessages([], 10), []);
  assert.deepEqual(recentMessages(undefined, 10), []);
});
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run: `node --test test/util.test.js`
Expected: FAIL — `recentMessages` не экспортирован.

- [ ] **Step 3: Добавить функцию в конец `js/util.js`**

```js
export function recentMessages(history, max = 20) {
  const arr = (Array.isArray(history) ? history : [])
    .slice(-max)
    .map((m) => ({ role: m.role, content: m.content }));
  while (arr.length && arr[0].role !== 'user') arr.shift();
  return arr;
}
```

- [ ] **Step 4: Запустить тест — убедиться, что проходит**

Run: `node --test test/util.test.js`
Expected: PASS — прежние + 3 новых теста зелёные.

- [ ] **Step 5: Commit**

```bash
git add js/util.js test/util.test.js
git commit -m "feat: recentMessages history trimming with tests"
```

---

## Task 2: Промпт и ответ наставника (`js/prompts.js`, `js/claude.js`)

**Files:**
- Modify: `js/prompts.js` (добавить константу)
- Modify: `js/claude.js` (расширить импорты + добавить функцию)

**Interfaces:**
- Consumes: `callClaude`, `recentMessages` (util.js), `CHAT_TUTOR_SYSTEM`.
- Produces: `chatReply(history, profile): Promise<string>` — текст ответа наставника.

- [ ] **Step 1: Добавить в конец `js/prompts.js`**

```js
export const CHAT_TUTOR_SYSTEM = `Ты — дружелюбный личный преподаватель испанского для русскоязычного ученика уровня A2–B1, живущего рядом с Бенидормом (Валенсийское сообщество).
Веди живой короткий диалог: отвечай по-русски, примеры давай на испанском. Объясняй просто, помогай разобраться, при необходимости задавай небольшое упражнение и проверяй ответ ученика. Если ученик пишет по-испански с ошибками — мягко поправь и покажи правильный вариант. Учитывай местный валенсийский/аликантийский узус. Не пиши длинных лекций — короткие, тёплые, полезные реплики.`;
```

- [ ] **Step 2: Расширить импорт `recentMessages` в `js/claude.js`**

Найти строку импорта из util (она вида `import { extractJson } from './util.js';`) и заменить на:
```js
import { extractJson, recentMessages } from './util.js';
```

- [ ] **Step 3: Расширить импорт промптов в `js/claude.js`**

В строке `import { ... } from './prompts.js';` добавить `CHAT_TUTOR_SYSTEM` в конец списка импортируемых констант.

- [ ] **Step 4: Добавить функцию в конец `js/claude.js`**

```js
export async function chatReply(history, profile) {
  const messages = recentMessages(history, 20);
  return callClaude({
    system: `${CHAT_TUTOR_SYSTEM}\nПрофиль ученика: ${JSON.stringify(profile)}`,
    messages,
    maxTokens: 700,
  });
}
```

- [ ] **Step 5: Проверка импорта в Node**

Run: `node --input-type=module -e "import('./js/claude.js').then(m=>console.log(typeof m.chatReply))"`
Expected: `function`.

- [ ] **Step 6: Commit**

```bash
git add js/prompts.js js/claude.js
git commit -m "feat: chatReply tutor conversation"
```

---

## Task 3: Экран «Чат» (`js/chat.js`)

**Files:**
- Create: `js/chat.js`

**Interfaces:**
- Consumes: `registerFeature` (app.js), `getSetting`/`setSetting` (db.js), `buildProfile` (profile.js), `chatReply` (claude.js), `escapeHtml` (util.js).
- Produces: экран `chat` (Чат, `order: 6`). История в `settings.chatHistory` = `[{role, content, ts}]`, хранится последние 60.

- [ ] **Step 1: Реализовать `js/chat.js`**

```js
import { registerFeature } from './app.js';
import { getSetting, setSetting } from './db.js';
import { buildProfile } from './profile.js';
import { chatReply } from './claude.js';
import { escapeHtml } from './util.js';

let busy = false;

async function getHistory() { return (await getSetting('chatHistory')) || []; }
async function saveHistory(h) { await setSetting('chatHistory', h.slice(-60)); }

function bubblesHtml(history) {
  const e = escapeHtml;
  if (!history.length) {
    return '<p class="status">Привет! Я твой наставник по испанскому. Спроси что угодно — объясню, помогу, потренирую. Можешь писать по-русски или по-испански.</p>';
  }
  return history.map((m) => `<div class="chat-msg chat-${m.role === 'user' ? 'me' : 'bot'}">${e(m.content)}</div>`).join('');
}

function scrollBottom() {
  window.scrollTo(0, document.body.scrollHeight);
}

function renderLog(container, history, typing) {
  const log = container.querySelector('#chat-log');
  if (!log) return;
  log.innerHTML = bubblesHtml(history) + (typing ? '<div class="chat-msg chat-bot chat-typing">…</div>' : '');
  scrollBottom();
}

async function send(container) {
  if (busy) return;
  const input = container.querySelector('#chat-input');
  const text = input.value.trim();
  if (!text) return;
  busy = true;
  input.value = '';
  const history = await getHistory();
  history.push({ role: 'user', content: text, ts: Date.now() });
  await saveHistory(history);
  renderLog(container, history, true);
  try {
    const profile = await buildProfile();
    const reply = await chatReply(history, profile);
    history.push({ role: 'assistant', content: reply, ts: Date.now() });
    await saveHistory(history);
    if (!container.querySelector('#chat-log')) return;
    renderLog(container, history, false);
  } catch (err) {
    if (container.querySelector('#chat-log')) {
      renderLog(container, history.concat([{ role: 'assistant', content: '⚠️ ' + err.message, ts: Date.now() }]), false);
    }
  } finally {
    busy = false;
  }
}

async function render(container) {
  container.innerHTML = `
    <h1>Наставник</h1>
    <div id="chat-log" class="chat-log"></div>
    <div class="chat-bar">
      <input id="chat-input" type="text" placeholder="Спроси наставника…" autocapitalize="sentences">
      <button id="chat-send">➤</button>
    </div>
  `;
  const history = await getHistory();
  if (!container.querySelector('#chat-log')) return;
  renderLog(container, history, false);
  container.querySelector('#chat-send').onclick = () => send(container);
  container.querySelector('#chat-input').addEventListener('keydown', (ev) => {
    if (ev.key === 'Enter') { ev.preventDefault(); send(container); }
  });
}

registerFeature({ id: 'chat', title: 'Чат', icon: '💬', order: 6, render });
```

- [ ] **Step 2: Commit**

```bash
git add js/chat.js
git commit -m "feat: tutor chat screen with persistent history"
```

---

## Task 4: Интеграция, кэш, стили, проверка

**Files:**
- Modify: `index.html`, `sw.js`, `css/styles.css`

- [ ] **Step 1: Подключить экран в `index.html`**

В блоке импортов после `import './js/teacher.js';` добавить строку:
```js
    import './js/chat.js';
```

- [ ] **Step 2: Обновить кэш в `sw.js`**

Заменить `const CACHE = 'espanol-v9';` на `const CACHE = 'espanol-v10';`.
В массив `SHELL` добавить (после `'./js/teacher.js',`):
```js
  './js/chat.js',
```

- [ ] **Step 3: Добавить стили в конец `css/styles.css`**

```css
.chat-log { display: flex; flex-direction: column; gap: 10px; padding-bottom: 8px; }
.chat-msg { max-width: 82%; padding: 10px 13px; border-radius: 16px; line-height: 1.45; white-space: pre-wrap; word-wrap: break-word; }
.chat-me { align-self: flex-end; background: linear-gradient(135deg, var(--accent), var(--accent-2)); color: #fff; border-bottom-right-radius: 5px; }
.chat-bot { align-self: flex-start; background: var(--card-2); border: 1px solid var(--border); border-bottom-left-radius: 5px; }
.chat-typing { opacity: .6; }
.chat-bar {
  position: sticky; bottom: calc(var(--nav-h) + env(safe-area-inset-bottom) + 6px);
  display: flex; gap: 8px; align-items: center; background: var(--bg); padding: 8px 0;
}
.chat-bar input { margin-top: 0; }
.chat-bar button { margin: 0; padding: 12px 16px; flex-shrink: 0; }
```

- [ ] **Step 4: Прогнать все тесты**

Run: `npm test`
Expected: PASS — все наборы зелёные (41 прежний + 3 новых `recentMessages` = 44), 0 провалов.

- [ ] **Step 5: Ручная проверка в Chrome**

Run: `npm start` (http://localhost:3000):
- В нижнем меню главные вкладки: Учитель · 💬 Чат · Учить · 5 слов · Словарь (+ «Ещё»).
- Чат: приветствие. Написать «Объясни разницу ser и estar» → появляется пузырь пользователя справа + «…» → приходит ответ наставника слева (по-русски, с примерами).
- Написать по-испански с ошибкой (`yo soy cansado`) → наставник мягко поправит (`estoy cansado`).
- Перезагрузить страницу → переписка на месте (DevTools → IndexedDB → `settings` → `chatHistory`).
- Без сети/ключа → в чат приходит понятное сообщение об ошибке, приложение не падает.

- [ ] **Step 6: Commit**

```bash
git add index.html sw.js css/styles.css
git commit -m "feat: wire chat screen, cache v10, chat styles"
```

---

## Self-Review

**Spec coverage (План 9 = «Чат с наставником»):**
- Живой диалог с наставником → Tasks 2, 3 (`chatReply`, экран). ✔
- Помнит профиль и историю → Task 3 (профиль в каждый запрос, история в `settings`). ✔
- Может объяснять/тренировать/поправлять → Task 2 (промпт). ✔
- История синхронизируется (в `settings`, без миграции) → Task 3. ✔
- Безопасный рендер (escapeHtml), guard после await, `busy` → Tasks 1, 3. ✔
- Первый ход в API — пользователя (robust trimming) → Task 1 (`recentMessages`). ✔
- Новая вкладка через реестр, `app.js` не тронут; офлайн-кэш → Task 4. ✔
- (Задания, План на сегодня — Планы 10–11.)

**Placeholder scan:** Полный код во всех шагах. Ошибки AI попадают в чат. Нет TODO без кода.

**Type consistency:** `recentMessages` (Task 1) → `chatReply` (Task 2); `CHAT_TUTOR_SYSTEM` (Task 2) → claude импорт Task 2; `chatReply` (Task 2) → Task 3; форма сообщения (`{role, content, ts}`) согласована между `getHistory`/`saveHistory`/`bubblesHtml`/`send` и маппингом в `recentMessages` (берёт только `role/content`). ✔
