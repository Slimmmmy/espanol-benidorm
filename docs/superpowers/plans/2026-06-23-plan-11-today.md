# Español Benidorm — План 11: План на сегодня (Implementation Plan)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Вкладка-дашборд «Сегодня» (стартовый экран) сводит воедино: стрик и повторение, 5 слов дня, следующий урок курса, активные задания — с быстрыми кнопками-переходами. Только чтение существующих данных, без новых AI-вызовов.

**Architecture:** Поверх Планов 1–10. Новый экран через реестр (order 4 → становится стартовым/первым). Данные из `getStats`, `getCourse`, `getAssignments`, daily-набор из `settings`. Чистая `summarizeToday(data)` с юнит-тестом; навигация через `location.hash`.

**Tech Stack:** HTML/CSS/JS (ES-модули, без сборки), IndexedDB, Node 20 `node --test`.

## Global Constraints

- Интерфейс на русском; без сборки; без бэкенда; ключи не коммитить.
- Любой динамический текст — в DOM только через `escapeHtml`.
- Обработчик после `await` проверяет экран (`#td-loading`).
- Чистая `summarizeToday` покрыта тестом; экран — ручной чек-лист. Существующие 47 тестов остаются зелёными.
- `js/app.js` НЕ менять. Вкладка «Сегодня» — `order: 4` (становится первой/стартовой; primary-набор: Сегодня, Учитель, Чат, Учить, 5 слов; Словарь и далее уходят в «Ещё»).
- Существующие интерфейсы:
  - `js/app.js`: `registerFeature(...)`; стартовый экран = модуль с наименьшим `order`; навигация по `location.hash = '#<id>'`
  - `js/stats.js`: `getStats()→{words,learned,due,streak,weak}`, `dayKey(ts)`
  - `js/profile.js`: `getCourse()`, `getAssignments()`
  - `js/db.js`: `getSetting`
  - `js/util.js`: `escapeHtml`
  - daily-набор дня лежит в `settings` по ключу `daily-<dayKey>` = `{date, words:[{...,added}]}`
- Рабочая директория: `/Users/nik/Downloads/EspanolBenidorm`, ветка `plan-11-today`.

---

## File Structure (этот план)

- `js/today.js` — экран «Сегодня» + чистая `summarizeToday` (экспорт, **тест**)
- `index.html` — подключить `today.js`
- `sw.js` — кэш `today.js` + версия `espanol-v12`
- `css/styles.css` — стили карточек дашборда
- `test/today.test.js` — тест `summarizeToday`

---

## Task 1: Экран «Сегодня» + `summarizeToday` (`js/today.js`)

**Files:**
- Create: `js/today.js`, `test/today.test.js`

**Interfaces:**
- Consumes: `registerFeature` (app.js), `getStats`/`dayKey` (stats.js), `getCourse`/`getAssignments` (profile.js), `getSetting` (db.js), `escapeHtml` (util.js).
- Produces: экран `today` (Сегодня, `order: 4`); экспортирует чистую `summarizeToday({ stats, course, assignments, daily }): { streak, due, dailyAdded, dailyTotal, nextUnitTitle, hasCourse, openAssignments }`.

- [ ] **Step 1: Написать падающий тест**

`test/today.test.js`:
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { summarizeToday } from '../js/today.js';

test('summarizeToday: собирает сводку', () => {
  const out = summarizeToday({
    stats: { streak: 3, due: 5 },
    course: { units: [{ status: 'done', title: 'A' }, { status: 'todo', title: 'B' }] },
    assignments: [{ status: 'open' }, { status: 'done' }, { status: 'open' }],
    daily: { words: [{ added: true }, { added: false }, { added: true }] },
  });
  assert.equal(out.streak, 3);
  assert.equal(out.due, 5);
  assert.equal(out.dailyAdded, 2);
  assert.equal(out.dailyTotal, 3);
  assert.equal(out.nextUnitTitle, 'B');
  assert.equal(out.hasCourse, true);
  assert.equal(out.openAssignments, 2);
});

test('summarizeToday: пустые данные → дефолты', () => {
  const out = summarizeToday({});
  assert.equal(out.streak, 0);
  assert.equal(out.due, 0);
  assert.equal(out.dailyAdded, 0);
  assert.equal(out.dailyTotal, 5);
  assert.equal(out.nextUnitTitle, '');
  assert.equal(out.hasCourse, false);
  assert.equal(out.openAssignments, 0);
});

test('summarizeToday: курс пройден → nextUnitTitle пустой, hasCourse true', () => {
  const out = summarizeToday({ course: { units: [{ status: 'done', title: 'A' }] } });
  assert.equal(out.hasCourse, true);
  assert.equal(out.nextUnitTitle, '');
});
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run: `node --test test/today.test.js`
Expected: FAIL — `Cannot find module '../js/today.js'`.

- [ ] **Step 3: Реализовать `js/today.js`**

```js
import { registerFeature } from './app.js';
import { getStats, dayKey } from './stats.js';
import { getCourse, getAssignments } from './profile.js';
import { getSetting } from './db.js';
import { escapeHtml } from './util.js';

export function summarizeToday({ stats, course, assignments, daily } = {}) {
  const dw = (daily && daily.words) || [];
  const dailyTotal = dw.length || 5;
  const dailyAdded = dw.filter((w) => w.added).length;
  const next = (course && course.units) ? (course.units.find((u) => u.status !== 'done') || null) : null;
  const open = (Array.isArray(assignments) ? assignments : []).filter((a) => a.status !== 'done').length;
  return {
    streak: (stats && stats.streak) || 0,
    due: (stats && stats.due) || 0,
    dailyAdded,
    dailyTotal,
    nextUnitTitle: next ? next.title : '',
    hasCourse: !!course,
    openAssignments: open,
  };
}

function card(icon, title, sub, btn, hash, done) {
  const e = escapeHtml;
  return `<div class="word-card td-card${done ? ' td-done' : ''}">
    <div class="td-row">
      <span class="td-icon">${icon}</span>
      <div class="td-text"><div class="word-main">${e(title)}</div><div class="muted">${e(sub)}</div></div>
      <button data-go="${e(hash)}">${e(btn)}</button>
    </div>
  </div>`;
}

async function render(container) {
  container.innerHTML = '<h1>Сегодня</h1><p class="status" id="td-loading">Загрузка…</p>';
  const stats = await getStats();
  if (!container.querySelector('#td-loading')) return;
  const course = await getCourse();
  const assignments = await getAssignments();
  const daily = await getSetting(`daily-${dayKey(Date.now())}`);
  const s = summarizeToday({ stats, course, assignments, daily });

  const dailyDone = s.dailyTotal > 0 && s.dailyAdded >= s.dailyTotal;
  const courseCard = s.nextUnitTitle
    ? card('👨‍🏫', 'Урок курса', s.nextUnitTitle, 'Начать', '#teacher', false)
    : card('👨‍🏫', 'Курс', s.hasCourse ? 'Курс пройден 🎉' : 'Программа ещё не создана', s.hasCourse ? 'Открыть' : 'Создать', '#teacher', s.hasCourse);

  container.innerHTML = `
    <h1>Сегодня</h1>
    <div class="study-card"><div class="daily-progress">🔥 Стрик: ${s.streak} дн. · на повтор: ${s.due}</div></div>
    ${card('🗓️', '5 слов дня', dailyDone ? `Готово: ${s.dailyAdded}/${s.dailyTotal}` : `Добавлено ${s.dailyAdded}/${s.dailyTotal}`, dailyDone ? 'Открыть' : 'Учить', '#daily', dailyDone)}
    ${card('🎓', 'Повторение', s.due > 0 ? `Карточек на сегодня: ${s.due}` : 'На сегодня всё повторено', s.due > 0 ? 'Повторять' : 'Открыть', '#study', s.due === 0)}
    ${courseCard}
    ${card('📝', 'Задания', s.openAssignments > 0 ? `Активных: ${s.openAssignments}` : 'Нет активных заданий', s.openAssignments > 0 ? 'Выполнить' : 'Получить', '#assignments', s.openAssignments === 0)}
  `;
  container.querySelectorAll('[data-go]').forEach((b) => { b.onclick = () => { location.hash = b.dataset.go; }; });
}

registerFeature({ id: 'today', title: 'Сегодня', icon: '☀️', order: 4, render });
```

- [ ] **Step 4: Запустить тест — убедиться, что проходит**

Run: `node --test test/today.test.js`
Expected: PASS — 3 теста зелёные.

- [ ] **Step 5: Commit**

```bash
git add js/today.js test/today.test.js
git commit -m "feat: Today dashboard with summarizeToday and tests"
```

---

## Task 2: Интеграция, кэш, стили, проверка

**Files:**
- Modify: `index.html`, `sw.js`, `css/styles.css`

- [ ] **Step 1: Подключить экран в `index.html`**

В блоке импортов сразу после `import { autoSync } from './js/sync.js';` (или перед `import './js/teacher.js';`) добавить:
```js
    import './js/today.js';
```

- [ ] **Step 2: Обновить кэш в `sw.js`**

Заменить `const CACHE = 'espanol-v11';` на `const CACHE = 'espanol-v12';`.
В массив `SHELL` добавить (рядом с другими `./js/*.js`, например после `'./js/teacher.js',`):
```js
  './js/today.js',
```

- [ ] **Step 3: Добавить стили в конец `css/styles.css`**

```css
.td-card .td-row { display: flex; align-items: center; gap: 12px; }
.td-icon { font-size: 26px; }
.td-text { flex: 1; min-width: 0; }
.td-card button { margin: 0; flex-shrink: 0; }
.td-done { opacity: .65; }
```

- [ ] **Step 4: Прогнать все тесты**

Run: `npm test`
Expected: PASS — все наборы зелёные (47 прежних + 3 новых `summarizeToday` = 50), 0 провалов.

- [ ] **Step 5: Ручная проверка в Chrome**

Run: `npm start` (http://localhost:3000):
- При открытии приложение стартует на вкладке ☀️ «Сегодня» (она первая в меню).
- Видны: строка «🔥 Стрик … · на повтор …» и 4 карточки: 5 слов дня (N/5), Повторение (карточек на сегодня), Урок курса (название следующего юнита или «Создать»/«пройден»), Задания (активных N).
- Кнопка на карточке переключает на нужную вкладку (`#daily`, `#study`, `#teacher`, `#assignments`).
- Выполненные пункты приглушены (td-done). Перезагрузка — данные актуальны; без сети не падает (только чтение из IndexedDB).

- [ ] **Step 6: Commit**

```bash
git add index.html sw.js css/styles.css
git commit -m "feat: wire Today dashboard as home, cache v12, styles"
```

---

## Self-Review

**Spec coverage (План 11 = «План на сегодня» — дирижёр):**
- Сводит 5 слов + урок курса + повторение + задания → Tasks 1 (`summarizeToday` + карточки). ✔
- Быстрые переходы на нужные экраны → Task 1 (`data-go` → `location.hash`). ✔
- Стартовый экран (order 4, наименьший) → Task 1. ✔
- Только чтение существующих данных, без новых AI → Task 1. ✔
- Чистая логика покрыта тестом → Task 1 (`summarizeToday`). ✔
- Безопасный рендер (escapeHtml), guard после await → Task 1. ✔
- Новая вкладка через реестр, `app.js` не тронут; офлайн-кэш → Task 2. ✔
- Завершает набор личного наставника (Курс + Чат + Задания + План на сегодня).

**Placeholder scan:** Полный код во всех шагах. Нет TODO без кода. Дашборд не делает сетевых вызовов — падать нечему.

**Type consistency:** `summarizeToday` (Task 1) использует формы `stats` (`{streak,due}`), `course` (`{units:[{status,title}]}`), `assignments` (`[{status}]`), daily (`{words:[{added}]}`) — все совпадают с тем, что отдают `getStats`/`getCourse`/`getAssignments`/`settings.daily-*` из Планов 4/8/10/5. Переходы `#daily/#study/#teacher/#assignments` соответствуют id зарегистрированных экранов. ✔
